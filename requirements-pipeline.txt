# Pipeline deps — needed to RE-RUN collect / classify / analyze locally.
# The deployed site is static (index.html + app.js + data.json) and needs NONE of these.
google-play-scraper>=1.2.7   # collect.py — Google Play reviews (keyless)
youtube-comment-downloader>=0.1.76  # collect.py — YouTube comments (keyless)
praw>=7.7                    # collect.py — Reddit (needs free REDDIT_CLIENT_ID/SECRET)
google-generativeai>=0.8    # classify.py — Gemini dual-hypothesis coding
pandas>=2.0                 # analyze.py — shares + per-segment matrix
